const React = require('react');
const ReactDOM = require('react-dom');
import css from './style.css';
import {ToggleButton} from './utils.jsx';

ReactDOM.render(<ToggleButton text='test' />, document.body);
